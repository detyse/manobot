> 不管是代码 还是想法 还是 commit, 都让模型统一管理

# 总体目标

就是简单的 nanobot 的多实例管理层. 各个实例间隔离. 不管是 gateway, config, workspace, provider 还是 channel 都各管各.

但是可以由 manobot 统一管理, agents 的 创建设置等等. manobot 只是一个 管理层, 可能负责自动分配 端口号之类的. 

项目中的 agent 就是 改名 + cli disable + 多实例特化的 nanobot.

nanobot 文件夹仅作为参考, 不参与运行. 